# Linking Rules

- Use Player ID as the permanent, stable identifier for every player.
- Use Player Name as the readable linked-record field.
- Connect workflow tables to the Colorado Rockies Base player table.
- Use lookup fields for baseline player information instead of manually duplicating it.
