# Player Development Rules

The Player Development table should maintain one current record per player.

Latest qualified reports should overwrite current-status fields rather than creating duplicate development records.

Historical reports should remain stored in their source report tables.
