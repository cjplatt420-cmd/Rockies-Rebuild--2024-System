# Airtable Schema

This document will describe the approved Airtable table list, fields, field types, links, and lookup relationships for the Rockies Rebuild 2024 system.

No import templates, formulas, automations, scripts, or code should be created until the schema is approved.
